// entry of all type definition.
import root from './root'
import allComment from './allComment'
import comment from './comment'
import result from './result'
import add from './add'

export const typeDefs = [root, allComment, comment, result, add]
